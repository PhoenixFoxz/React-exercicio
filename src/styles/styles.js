import styled from "styled-components";

const StyledCabecalho = styled.div`
  height: 300px;
`
;

const StyledLogo = styled.div`
  text-align: center;
`
;

const StyledNav = styled.nav`
  text-align: center;
`
;

export { StyledCabecalho, StyledLogo, StyledNav };